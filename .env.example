# Absolute origin of the deployed site.
# Used for canonical URLs, the sitemap, and Open Graph asset resolution.
#
# Optional - src/lib/site.ts already defaults to the production origin below.
# Set this only to override it (preview deployments, a custom domain).
NEXT_PUBLIC_SITE_URL=https://tausifsodyssey.vercel.app
